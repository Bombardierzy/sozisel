query WhiteboardSummary($id: ID!){
  whiteboardSummary(id: $id) {
    id
    averageUsedTime
    task
    participantsWhiteboardTasks {
        imagePath
        fullName
        email
        additionalText
    }
  }
}