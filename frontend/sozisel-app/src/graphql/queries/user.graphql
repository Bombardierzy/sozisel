query Me {
  me {
    id
    email
    lastName
    firstName
    id
  }
}

query GenerateJitsiToken($displayName: String!, $email: String!, $roomId: ID!) {
  generateJitsiToken(displayName: $displayName, email: $email, roomId: $roomId) {
    token
  }
}