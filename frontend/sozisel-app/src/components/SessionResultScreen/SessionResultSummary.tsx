import { ReactElement } from "react";

interface SessionResultSummaryProps {
  sessionId: string;
}

export default function SessionResultSummary(
  props: SessionResultSummaryProps
): ReactElement {
  return <>summary</>;
}
